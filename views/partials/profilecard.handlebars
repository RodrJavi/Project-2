<div class="flex justify-center">

  <div
    class="bg-white rounded-lg border-2 p-4 m-2 text-right"
    id="profile-side"
  >

    <h1 class="bg-black text-white text-xl text-center" id="profilecard-h1">
      {{user.displayName}}
    </h1>
    <h3 class="text-white text-sm text-center" id="profilecard-h3">
        @{{user.username}}
    </h3>
    <h3 class="text-white text-sm text-center" id="profilecard-h3">
        {{user.email}}
    </h3>

  </div>
</div>